{
  "name": "brazilian-identity-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Brazilian identity verification SBT system",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "verify": "hardhat verify",
    "coverage": "npx hardhat coverage",
    "lint": "solhint 'contracts/**/*.sol'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^4.9.3",
    "hardhat": "^2.19.0",
    "solhint": "^4.1.1"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}